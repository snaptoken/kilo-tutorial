/* # The `main()` function
 *
 * Create a new file named `kilo.c` and give it a `main()` function to start
 * out with.
 */

int main() {
  return 0;
}

/* Explain:
 *   - `main()` is a special function
 *   - `return 0` exits the program with a successful exit status code
 *   - How to compile and run, using `make`
 */

